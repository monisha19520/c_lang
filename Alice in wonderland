#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a;
    scanf("%d",&a);
    printf("Bird said:%d\nAlice must go in path-%d",a,(a/10)+(a%10));

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
